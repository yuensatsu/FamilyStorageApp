# AGENTS.md

## Cursor Cloud specific instructions

This repository (`FamilyStorageApp`) is currently an empty scaffold with no application code, dependencies, or services. The `main` branch contains only a `README.md`.

### Repository state (as of initial setup)
- No package manager lockfiles or dependency manifests exist.
- No build system, test framework, or linter is configured.
- No services or databases are required.
- Remote branches (`cursor/ai-game-dev-workflow-9868`, `cursor/gdd-template-80ba`) contain only markdown documentation — no code.

### For future agents
- Once application code is added, update this file with instructions for running, testing, and linting the project.
- Update the VM environment update script (via `SetupVmEnvironment`) to install the project's dependencies.
